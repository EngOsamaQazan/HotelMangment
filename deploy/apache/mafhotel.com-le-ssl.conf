<VirtualHost *:443>
    ServerName mafhotel.com
    ServerAlias www.mafhotel.com

    # Redirect www → apex (keeps single canonical)
    RewriteEngine On
    RewriteCond %{HTTP_HOST} ^www\. [NC]
    RewriteRule ^(.*)$ https://mafhotel.com$1 [R=301,L]

    # Reverse proxy
    ProxyPreserveHost On
    ProxyRequests Off
    ProxyTimeout 60

    # ── Socket.IO / WebSocket → realtime service on :3001 ──────
    # Client connects to /socket.io/ at same origin.
    RewriteCond %{HTTP:Upgrade} =websocket [NC]
    RewriteRule ^/socket\.io/(.*) ws://127.0.0.1:3001/socket.io/$1 [P,L]
    ProxyPass        /socket.io/ http://127.0.0.1:3001/socket.io/
    ProxyPassReverse /socket.io/ http://127.0.0.1:3001/socket.io/

    # Main app (Next.js on :3000)
    ProxyPass        / http://127.0.0.1:3000/
    ProxyPassReverse / http://127.0.0.1:3000/

    # SSL
    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/mafhotel.com/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/mafhotel.com/privkey.pem
    Include /etc/letsencrypt/options-ssl-apache.conf

    # Security headers
    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains"
    Header always set X-Content-Type-Options "nosniff"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    Header always set Permissions-Policy "camera=(), microphone=(), geolocation=()"

    # Large uploads / long timeouts
    LimitRequestBody 52428800

    ErrorLog ${APACHE_LOG_DIR}/mafhotel.com-ssl-error.log
    CustomLog ${APACHE_LOG_DIR}/mafhotel.com-ssl-access.log combined
</VirtualHost>
